import { createRoutine } from 'redux-saga-routines';

export const createUser = createRoutine('app/SignupPage/CREATE_USER');
export const confirmEmail = createRoutine('app/SignupPage/CONFIRM_EMAIL_ADDRESS');
