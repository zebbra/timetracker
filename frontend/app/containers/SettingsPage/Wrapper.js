import styled from 'styled-components';

const InfoIconWrapper = styled.section`
  > svg {
    cursor: help;
    width: 20px !important;
    height: 20px !important;
  }
`;

export default InfoIconWrapper;
