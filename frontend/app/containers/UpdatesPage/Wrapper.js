import styled from 'styled-components';


const Wrapper = styled.section`
  > section > div {
    width: 100% !important;
  }

  .cardWrapper {
    margin-bottom: 10px;
  }
`;

export default Wrapper;
