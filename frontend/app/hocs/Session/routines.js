import { createRoutine } from 'redux-saga-routines';

export const deleteSession = createRoutine('hocs/Session/DELETE_SESSION');
export const impersonate = createRoutine('app/ImpersonatePage/IMPERSONATE');
