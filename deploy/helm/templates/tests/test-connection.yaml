apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timetracker.fullname" . }}-test-connection"
  labels:
    {{- include "timetracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timetracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
