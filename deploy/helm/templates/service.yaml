apiVersion: v1
kind: Service
metadata:
  name: { { include "timetracker.fullname" . } }
  labels: { { - include "timetracker.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "timetracker.selectorLabels" . | nindent 4 } }
